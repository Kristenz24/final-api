# application.properties
spring.application.name=social_media_be

# Remove the default values since we're using .env
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Optimized JPA/Hibernate settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Remove these as they're not needed
# spring.datasource.driver-class-name=org.postgresql.Driver (auto-detected)
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect (auto-detected)